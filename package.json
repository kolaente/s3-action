{
  "name": "s3-upload-action",
  "version": "1.0.0",
  "description": "GitHub Action to upload files to S3 compatible storage.",
  "main": "index.js",
  "scripts": {
    "build": "ncc build index.js -o dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "github",
    "actions",
    "s3",
    "upload",
    "storage"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/glob": "^0.4.0",
    "minio": "^7.1.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.1"
  }
}
